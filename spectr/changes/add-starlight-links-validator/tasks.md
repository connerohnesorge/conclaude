# Tasks: Add starlight-links-validator Plugin

## 1. Installation
- [x] 1.1 Add `starlight-links-validator` to `docs/package.json` dependencies
- [x] 1.2 Run `npm install` in `docs/` directory to update lockfile

## 2. Configuration
- [x] 2.1 Import `starlightLinksValidator` in `docs/astro.config.mjs`
- [x] 2.2 Add plugin to Starlight's `plugins` array

## 3. Verification
- [x] 3.1 Run `npm run build` in `docs/` to verify plugin loads and validates links
- [x] 3.2 Confirm build succeeds with no broken link errors (or fix any existing broken links)
